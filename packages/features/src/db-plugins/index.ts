/**
 * DB seed-plugin registry core (register + FK-ordered seed/clear lifecycle),
 * salvaged from mxscratchpad. The placeholder plugin bodies are a reference
 * spec (`docs/reference-specs/db-plugins.md`).
 */

export * from './registry.ts'
