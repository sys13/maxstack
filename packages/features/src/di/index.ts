/**
 * Typed DI bindings core (framework-agnostic contract + missing-binding guard),
 * salvaged from mxscratchpad's `DI.md`. The React provider/`useBindings` wiring
 * lives in `@maxstack/ui`.
 */

export * from './bindings.ts'
