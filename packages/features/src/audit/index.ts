/**
 * Audit-log core — the db-agnostic seam salvaged from mxscratchpad's
 * `metrics.ts`. The metrics aggregations are a reference spec
 * (`docs/reference-specs/metrics.md`); this ships the audit sink.
 */

export * from './audit-log.ts'
