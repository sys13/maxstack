/**
 * Admin metrics — db-agnostic count/trend aggregations over the auth + audit
 * tables (the slice deferred from task 5's `metrics.ts` staging). See
 * `docs/reference-specs/metrics.md`.
 */

export * from './aggregations.ts'
