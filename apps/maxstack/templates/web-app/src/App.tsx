export function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>{{PROJECT_NAME}}</h1>
      <p>{{DESCRIPTION}}</p>
      <p>
        Scaffolded by maxstack. Next: open <code>prd.ts</code> and run{" "}
        <code>/plan-and-scope</code>.
      </p>
    </main>
  );
}
